{
  "name": "dota-vod-processor",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @dota-vod/shared build && concurrently --kill-others-on-fail \"docker run --rm -p 6379:6379 redis:alpine\" \"pnpm -r --parallel run dev\"",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint && ruff check services/"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "concurrently": "^10.0.4",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  }
}
